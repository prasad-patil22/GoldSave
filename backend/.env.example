PORT=5000
MONGODB_URI=mongodb://localhost:27017/savegold
JWT_SECRET=your_jwt_secret_here_make_it_long_and_random
JWT_EXPIRE=30d

# Email SMTP configuration (Nodemailer fallback will log to console if these are blank)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=585
SMTP_SERVICE=gmail
SMTP_USER=
SMTP_PASS=

# Cloudinary configuration (Multer local fallback will save to disk if these are blank)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Frontend URL
FRONTEND_URL=http://localhost:5173
